# Godot project cache (regenerated by `godot --import`).
.godot/

# Spine for Godot extension binaries — install separately, see README.
bin/

# Spine demo assets — not redistributed here, copy from spine-runtimes locally.
# See README for setup.
assets/

# OS junk.
.DS_Store
Thumbs.db
desktop.ini
